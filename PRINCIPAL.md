# エンジニアリング・プリンシプル (PRINCIPAL.md)

本ドキュメントは、`chunisupport-song-batch` プロジェクトにおけるアーキテクチャ設計、コーディング標準、および品質保証の指針を定めたものです。
今後の開発およびリファクタリングは、本ドキュメントに記載された原則に従って実施してください。

## 1. アーキテクチャ設計原則

### 1.1 ドメイン駆動設計 (DDD) の採用
*   **ドメイン貧血症の脱却:** データのみを持つ構造体 (DTO) とロジックのみを持つサービス (Service) への分離を避け、データと振る舞いをカプセル化した「リッチドメインモデル」を構築してください。
*   **整合性の保証:** エンティティは、自身の整合性を維持する責任を持ちます。不正な状態（例: 負のBPM、存在しない難易度ID）を持つインスタンスが生成されないよう、コンストラクタやファクトリメソッドでバリデーションを行ってください。
*   **依存関係の逆転 (DIP):** ドメイン層はインフラ層（DB、外部API）に依存してはいけません。インターフェース（Repository interface等）をドメイン層で定義し、インフラ層でそれを実装してください。

### 1.2 開放閉鎖原則 (OCP)
*   **拡張に対して開き、修正に対して閉じる:** 新しい機能（例: 新規データソースの追加）を実装する際、既存のコード（特に `main.go` や `ConsolidationService` のようなコアロジック）を修正せずに済む設計を心がけてください。
*   **実装パターン:** ストラテジーパターンやレジストリパターンを活用し、共通インターフェースを介してプラグイン可能な構成にしてください。

## 2. パフォーマンスとリソース管理

### 2.1 メモリ効率とI/O
*   **ストリーム処理の徹底:** ファイルサイズが予測できない、あるいは増大する可能性があるデータを扱う際は、決して `io.ReadAll` を使用しないでください。必ず `io.Copy` や `json.Decoder` などのストリームAPIを使用し、固定バッファサイズで処理してください。
*   **巨大オブジェクトの回避:** 数万件のレコードをスライスに展開する際は、ポインタを活用するか、バッチ処理（チャンク分割）を行い、GCへの負荷を考慮してください。

### 2.2 計算リソースの最適化
*   **ループ内の高コスト処理の禁止:** ループ内部で `template.Parse`、`regexp.MustCompile`、データベース接続の確立などを行わないでください。これらは初期化時に一度だけ実行し、再利用してください。
*   **N+1問題の回避:** データベースアクセスにおいて、ループ内でクエリを発行する N+1 問題を発生させてはいけません。`IN` 句を用いたバルクフェッチや、バルクインサート/アップサートを活用してください。

## 3. コーディングスタンダード

### 3.1 安全性と堅牢性
*   **SQLインジェクション対策:** 文字列連結やテンプレート展開による動的SQLの構築は原則禁止です。`sqlx` などのライブラリが提供するプレースホルダ機能や、安全なクエリビルダを使用してください。
*   **エラーハンドリング:** エラーは単にログに出力するだけでなく、適切なコンテキスト（スタックトレースや発生箇所の情報）を付与してラップ (`fmt.Errorf("...: %w", err)`) してください。

### 3.2 可読性と保守性
*   **マジックナンバー・マジックストリングの排除:** コード内の定数値（URL、ファイルパス、ID定数など）は、適切な名前を持つ定数として定義してください。
*   **命名規則:** Go の標準的な命名規則（CamelCase）に従ってください。ファイル名はスネークケース、パッケージ名は小文字の単語を使用してください。
*   **DRY (Don't Repeat Yourself):** 類似したロジックのコピー＆ペーストは禁止です。共通化できるロジックは関数やメソッドとして切り出してください。

## 4. 最新技術の追従
*   **Goバージョンの維持:** 本プロジェクトは Go 1.26.x 系（最新パッチバージョン）を使用します。`min`/`max` などの組み込み関数や、最新の標準ライブラリ機能を積極的に活用し、外部依存を減らしてください。

---
**このドキュメントは生き物です。** 開発プロセスにおける学びや技術の進化に合わせて、チームの合意のもと随時更新してください。
